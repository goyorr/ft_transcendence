'use client'

import { createContext } from 'react';

export const WebSocketContext = createContext<any>("");

export const TourSocketContext = createContext<any>("");

export const MultiSocketContext = createContext<any>("");
